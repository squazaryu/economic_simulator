# Экономический симулятор сценариев для рынка РФ

Интерактивный Streamlit-проект для моделирования влияния макрофакторов на:
- индекс **IMOEX**,
- выбранную **акцию MOEX** (тикер вводится пользователем).
- **портфель MOEX** (2-5 акций с весами).

## Ключевые возможности

- Загрузка реальных данных за последние 5 лет (месячная частота).
- Прогноз по сценарию для IMOEX и отдельной акции.
- Прогноз по сценарию для портфеля акций.
- Расширенные сценарные параметры:
  - Brent,
  - ключевая ставка,
  - USD/RUB,
  - инфляция,
  - рыночный сентимент,
  - ликвидность,
  - геополитический фактор,
  - регуляторный фактор,
  - масштаб волатильности для Monte Carlo.
- Monte Carlo (10 000 сценариев) с P5/P50/P95 и вероятностью падения >20%.
- Sobol-анализ чувствительности (S1) и Tornado Chart.
- Интерактивный интерпретатор результатов:
  - клик по столбцу гистограммы Monte Carlo,
  - клик по фактору в Tornado Chart Sobol,
  - техпаспорт расчета с формулами и числовой подстановкой.
- Подробные пояснения на каждой вкладке интерфейса.
- Режимы модели: весь период, до февраля 2022, с февраля 2022.
- Динамический веерный прогноз на 6-12 месяцев (траектория цены/индекса, P10-P90).
- Side-by-side сравнение сценариев: базовый / оптимистичный / пессимистичный.
- Авто-комментарий по драйверам прогноза.
- Режим `CPU Saver` для ускорения тяжелых расчетов в UI.

## Источники данных

1. **ЦБ РФ**
- `cbrapi`: ключевая ставка, USD/RUB.
- Официальная таблица ЦБ: инфляция `https://www.cbr.ru/hd_base/infl/`.

2. **MOEX ISS API**
- История IMOEX.
- История выбранной акции MOEX.

3. **Yahoo Finance (`yfinance`)**
- Brent (`BZ=F`).

## Структура проекта

```text
economic_simulator/
├── data/
│   ├── raw/
│   └── processed/
├── models/
├── src/
│   ├── __init__.py
│   ├── system_apis.py
│   ├── engine.py
│   ├── service.py
│   ├── data_loader.py
│   ├── preprocessing.py
│   ├── model.py
│   ├── monte_carlo.py
│   ├── sensitivity.py
│   └── interpreter.py
├── tests/
│   └── test_streamlit_smoke.py
├── app.py
├── requirements.txt
├── .gitignore
└── README.md
```

## Установка и запуск

Требуется Python 3.11+

```bash
cd economic_simulator
python3.11 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
streamlit run app.py
```

## Архитектура

Проект приведен к цепочке:
**UI → Service → Engine → System APIs**

- **UI** (`app.py`) отвечает только за взаимодействие с пользователем, виджеты и визуализацию.
- **Service** (`src/service.py`) оркестрирует сценарии UI, подготавливает параметры и вызывает вычислительные контуры.
- **Engine** (`src/engine.py`) запускает вычисления Monte Carlo и Sobol.
- **System APIs** (`src/system_apis.py`) инкапсулирует доступ к внешним API/данным и модельным артефактам.

Такое разделение упрощает тестирование и снижает связность UI с вычислительным кодом.

## Логика модулей

1. `src/data_loader.py`
- Загрузка CBR/MOEX/Brent.
- Сохранение в `data/raw/`.
- Fallback на локальные CSV при недоступности API.
- Поддержка загрузки произвольного тикера MOEX.

2. `src/preprocessing.py`
- Очистка и нормализация дат.
- Интерполяция и заполнение пропусков.
- Сохранение итогового датасета в `data/processed/`.

3. `src/model.py`
- Отдельная модель для IMOEX.
- Отдельная модель для выбранной акции.
- Режимные версии моделей (all/pre_2022/post_2022).
- Метрики `R²`, `MAE`, `RMSE`.
- Сценарные поправки (сентимент/ликвидность/геополитика/регуляторика).
- Декомпозиция драйверов прогноза.

4. `src/monte_carlo.py`
- Симуляция неопределенности по макрофакторам.
- Поддержка IMOEX, выбранной акции и портфеля.
- Учет режимов модели и дополнительных поправок.
- Детализация выбранного бина: вероятность, состав сценариев, средние значений факторов.

5. `src/sensitivity.py`
- Sobol-анализ с `SALib.sample.sobol` (first-order, `calc_second_order=False`).
- Tornado Chart с сортировкой факторов по S1.
- Поддержка IMOEX/акции/портфеля и режимов модели.

6. `app.py`
- 4 вкладки: история, сценарий, Монте-Карло, чувствительность.
- Подробный текст-описание на каждой вкладке.
- Полная русификация пользовательского интерфейса.
- Динамический график будущей траектории и веер неопределенности.
- Сравнение сценариев и портфельный режим.

7. `src/service.py`
- Принимает контекст UI и маршрутизирует запросы в engine/system_apis.
- Держит бизнес-правила сборки параметров симуляций.

8. `src/engine.py`
- Единая точка для вычислительных движков Monte Carlo и Sobol.

9. `src/system_apis.py`
- Единая точка для загрузки датасетов, истории тикеров и модельных артефактов.

10. `src/interpreter.py`
- Интерпретация результатов Monte Carlo/Sobol в человеко-понятных терминах.
- Генерация расширенного объяснения по выбранному бину/фактору с техническими деталями расчета.

## Тесты

Добавлены smoke-тесты Streamlit:

- Monte Carlo строит ключевые метрики и блок детализации.
- Sobol строит блок детализации и итоговое сообщение по главному фактору.
- Интерпретатор обновляется при переключении выбранного диапазона бина.
- Проверка стабильного рендера с включенной интерактивной детализацией.

Запуск:

```bash
pytest -q
```

## Ограничения

- Линейная регрессия отражает только линейные зависимости.
- Дополнительные сценарные поправки применяются как пост-коррекция прогноза.
- При сбоях API используются последние локально сохраненные срезы.
- Sobol S1 отражает вклад факторов первого порядка и не учитывает взаимодействия высших порядков.
